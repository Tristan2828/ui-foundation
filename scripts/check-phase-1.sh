#!/usr/bin/env bash
# Exit criteria for Phase 1 — Scaffold and Tokens.
# Placeholder committed in Phase 0 per the plan ("even a two-line one, so the
# pattern exists before the agent's first session"). Filled in for real when
# Phase 1 is executed.
set -euo pipefail
cd "$(dirname "$0")/.."
echo "check-phase-1: not yet implemented — Phase 1 has not run" >&2
exit 1
