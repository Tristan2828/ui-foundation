node_modules/
dist/
dist-ssr/
storybook-static/
debug-storybook.log
*.local
logs/
test-results/
playwright-report/
blob-report/
playwright/.cache/
.playwright-mcp/
.env
.env.*
!.env.example
!.env.real

.claude/settings.local.json

# Phase 8 backend (Python)
backend/.venv/
backend/__pycache__/
backend/**/__pycache__/
backend/.mypy_cache/
backend/.pytest_cache/
backend/*.egg-info/

# Phase 12 cloud Postgres — provider CA certs are per-developer, not shipped
backend/certs/
# Supabase CLI's own local link-cache (project ref, org id) — regenerated
# by `supabase link`/`supabase projects create`, not project state
supabase/

