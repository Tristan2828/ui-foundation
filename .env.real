# `vite build --mode real` (npm run build:real) loads this: a production
# bundle that talks to the real backend. VITE_API is baked in at build time,
# so a plain `npm run build` ships MSW and serves mock data (that build is
# what Playwright tests). Not a secret; committed on purpose (see .gitignore).
VITE_API=real
