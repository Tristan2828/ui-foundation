# Deferred and Excluded

Out-of-scope ideas land here, never in code. See `docs/BUILD-PLAN.md` for the
full rationale behind each row.

## Deferred

| Item | Revisit when |
|---|---|
| Login rate limiting / lockout on repeated failed attempts | The app is exposed somewhere a brute-force attempt is a real threat model, not a personal/local deployment. **Still deferred as of Phase 11** (docs/phases/phase-11.md) — flagged there as a real gap self-service registration widens, not yet picked up |
| Additional themes | A second app needs a distinct look |
| Monorepo | Two or more consuming apps share a release cycle |
| TypeScript 7 (native compiler) | typescript-eslint, openapi-typescript and Storybook's react-docgen-typescript all support it. TS 7's package exposes no classic JS API (`require('typescript')` has only `version`), and all three are built on that API — typescript-eslint caps at `<6.1` even in its v9 alpha. The tsconfigs are already TS 7-clean (no `baseUrl`, which TS 7 drops). When revisiting, also drop the `openapi-typescript` → `typescript` entry in `package.json`'s `overrides` once openapi-typescript's own peer range covers the installed TypeScript |
| Read-only entity path in the playbook (`/new-entity` for a list/detail table with no create, edit or delete — today it always builds full CRUD, and the near-term focus is display tables; see `docs/ARCHITECTURE.md` "Focus") | The first real app needs a read-only table. Build it against that app's actual screen, not in advance |
| Notion API as a data source — a thin backend that serves `openapi.yaml` from a Notion database instead of Postgres (the choice is documented in `docs/consuming.md`) | A real project picks Notion as its source of truth. Build it against that project's actual database and property types, not a hypothetical one |
| Row virtualization | A table exceeds ~5k rows |
| `AppError` kinds for 403 (`forbidden`) and 409 (`conflict`) — today both render as the generic `server` error | A backend actually returns either. None does now: another user's widget is a deliberate 404 and a duplicate email is a 422. Considered and not built in audit Phase D (`docs/phases/audit-phase-d.md`) |
| Error reporting | An app is actually deployed |
| Dependency-allowlist enforcement for `backend/pyproject.toml`, mirroring `deps-allowlist.json`/`check-deps.mjs` on the npm side | The backend gains a second contributor/session where an unreviewed Python dependency is a real risk — Phase 8 pinned versions by hand with no mechanical gate |
| Column-level documentation convention for the Postgres backend — `COMMENT ON TABLE`/`COMMENT ON COLUMN` on every table, required whenever a column's meaning isn't obvious from its name/type. No existing convention (zero `COMMENT ON` usage anywhere in `backend/` today) and no mechanical check. Found while planning a Game List app whose `game_ratings` table needs to be understood by a second, context-free agent with direct read-write Postgres access and no view of `openapi.yaml` or this repo | That app's migration actually ships `COMMENT ON` for real (its own plan documents the convention and a Row-Level Security policy alongside it); once proven there, decide whether it graduates to a Hard Rule with a mechanical check across `backend/`, the way other Hard Rules are enforced |
| `app-shell.tsx`'s `SidebarHeader` brand text (`<span>UI Foundation</span>`, ~line 100) has no collapse-aware styling — collapsing the sidebar to its icon-only rail (`collapsible="icon"`) doesn't hide or truncate it, so the text overflows past the narrowed rail. Found while building the Game List app with a separate agent | Next session in this repo — small, concrete fix, not a design decision; pair with the password-visibility item below |
| No password-visibility toggle on `login.tsx`/`register.tsx`'s password `Input`s (plain `type="password"`, no show/hide). `lucide-react`'s Eye/EyeOff icons are already a dependency — no new package needed. Check shadcn's registry for an existing password-input pattern first (`AGENTS.md` Hard Rule: never hand-roll what shadcn already has) before composing one from `Input` + `Button`. Found while building the Game List app with a separate agent | Same session as the sidebar-collapse fix above |
| Default-sort support in `DataTable`/the entity playbook — `docs/entities/_template.md`'s "Default sort" field has no working reference. `Widget` doesn't use one, so nothing here demonstrates the trap: a caller-side fallback (`sorting.length > 0 ? sorting : DEFAULT_SORTING`) conflicts with TanStack's own sort cycle (asc → desc → unsorted) — the third click's "unsorted" is immediately overridden back to the same default, so the header looks stuck. Fix is an opt-in `enableSortingRemoval` prop on `DataTable` (default unchanged, so `Widget`'s table is unaffected) plus a note at the template's "Default sort" line. Found while building the Game List app with a separate agent | That app's own `Game` table actually ships a default sort (it does — "status desc") and hit this exact bug, fixed locally (`enableSortingRemoval: false` hardcoded there, not yet generalized as an opt-in prop here) |
| Zebra striping on `TableBody` — a subtle alternating row tint, scoped so the header row is never affected. Found while building the Game List app with a separate agent | Purely additive polish; validated in that app's 122-row Games table, low-risk to add whenever picked up |
| Persistent captions above each toolbar filter in `widgets-table.tsx` — a `Select`'s own placeholder text (e.g. "Status") disappears once a value is picked, losing which filter it is. Found while building the Game List app with a separate agent | Validated fix in that app; belongs in `widgets-table.tsx` itself (not a new shared component) since every entity's toolbar is copied from that file, per `add-an-entity.md` |
| `vitest`/`eslint` recursively scan into a nested git worktree under the project root (e.g. `.claude/worktrees/<name>`), failing `verify` on another session's in-progress files rather than ignoring them. Found while building the Game List app with a separate agent | Given this repo is built mostly by AI agents (`ARCHITECTURE.md`), a worktree existing under the project root during a session is a realistic, recurring scenario, not hypothetical — that app hit this and fixed it by adding both configs' directory to their `exclude`/`ignores` lists |
| No semantic status/success/warning color tokens — `theme.css` is grayscale plus `destructive`, and the Hard Rule bans palette colors, so a consuming app has no sanctioned way to color-code a value like a game's Status or review sentiment (green = playing, red = dropped). The existing `Badge` variants are the only signal, and `games-columns.tsx` already notes the shipped `destructive` badge fails AA contrast. Surfaced while exploring table label styles in the Game List app: a reference mock used solid/tinted green and red labels, and the only compliant answer was "grayscale badges, no color". Any addition needs a second, darker text shade per color in both themes (tinted fills can't reuse the fill color as text — red-500 on a near-white tint is ~3.8:1, under 4.5:1) plus axe coverage in `e2e/a11y.spec.ts` | A consuming app decides it actually wants color-coded labels. Ask for the display style first (see the exploration-step item below) so tokens are added for a chosen style, not speculatively |
| "Choose how each column displays" step in `docs/add-an-entity.md` — today the playbook takes an entity plan's fields and builds the columns, with no step for deciding how a value should look (plain text, badge, icon badge, meter, bucketed label…). A developer who doesn't have design opinions (the Game List author is a backend dev) had to run this ad hoc: an agent generated a shareable options page per column, rendering the same sample rows in each style using the app's own tokens, with a one-line tradeoff each, and the developer picked one. That worked well and is repeatable. Worth capturing as an optional playbook step or a `/explore-column` command, and could seed a small catalog of cell patterns (number, enum, count, date, link) that `games-columns.tsx`-style files copy from. First outcome: Max Players went from a bare number to an outline `Badge` with a `UsersIcon` (`src/routes/games/games-columns.tsx` in the Game List app, branch `claude/table-display-exploration-92652c`), matching the Mechanics chips | More columns get picked the same way in the Game List app — if a few settle on the same handful of patterns, extract those; if not, keep it as guidance only. Don't build the catalog from one column |
| Icon badge as a documented cell pattern (a `Badge variant="outline"` with a leading lucide icon, e.g. a count column: `<Badge variant="outline"><UsersIcon data-icon="inline-start" /><span className="tabular-nums">{n}</span></Badge>`). **Deliberately not a new component:** shadcn's `Badge` already sizes child `svg`s and pads for `data-icon="inline-start"|"inline-end"`, and the Hard Rules ban hand-rolling what shadcn has, so a wrapper would only duplicate it. What's missing is discoverability — nothing in `docs/` or the reference implementations shows an icon in a badge or says when to use one. Candidate homes: a cell-patterns note next to the column-display playbook step above, or a short example in `docs/add-an-entity.md`. Reference use: Max Players in the Game List app (`src/routes/games/games-columns.tsx`, branch `claude/table-display-exploration-92652c`) | Same session as the column-display playbook step above — write it as one of that step's catalog entries, not on its own |
| Icon-only cell as a documented pattern — an enum column shown as a glyph with no visible label (Game List's Controller Support column, chosen over eight other styles), with the label moved to a `Tooltip` and an `aria-label`. Two non-obvious bits a future agent would get wrong and nothing in `docs/` shows: (1) Base UI's `TooltipTrigger` renders a `<button>` by default — a read-only cell icon should instead use `render={<span role="img" aria-label={label} tabIndex={0} />}` so it is keyboard-focusable and named without being a fake button; (2) it depends on the `TooltipProvider` that `app-shell.tsx` already mounts, so it works in any table cell for free but not in an isolated Storybook story without one. Also documents the shape-not-color rule for such icons (fill level: solid / half / slashed / dashed) so the column still reads for a colorblind viewer, the same reasoning `games-columns.tsx` already gives for the Reviews icon. Deliberately not a component — the glyph is per-entity; only the wrapper markup is reusable. Reference: `src/routes/games/controller-support-icon.tsx` and its use in `games-columns.tsx` in the Game List app, `master`, commit `3e88422` (first shown as option 7 of 9 on the Controller Support exploration page) | Same session as the column-display playbook step and the icon-badge pattern above — write it as a third catalog entry ("icon only, label in tooltip") beside "icon in a badge". Icon-only is the choice a second column would repeat (Crossplay is the likely one), so wait for that before extracting anything |
| A third semantic tone: **warning/caution** (amber). The status-color item above covers success and destructive, and the Game List app has since added `success-text`/`destructive-text` tokens and `outline-success`/`outline-destructive` `Badge` variants locally (commits `02f3c4c`, `92d9724`). Ranking a three-tier value — Controller Support Full / Partial / None is the clean example — wants a middle tone; the app first reused `success-text` for Partial, leaning on the half-filled shape alone — and Partial rendered green like Full, which the developer flagged immediately. It now has a local `--warning-text` token (amber-700 light / amber-400 dark, exposed as `text-warning-text`; `src/styles/theme.css`, `src/index.css`, `master` commit f920a47) that passes the app's axe contrast checks in both themes; use those as the starting shades. Same requirement as the other tones: a darker text shade in both themes plus axe coverage before any color is added | The status-color item above gets actioned — add warning in the same pass so the three tones are designed together, or a consuming app hits a value that genuinely needs a middle color and the shape-only workaround stops being enough |
| Format of the per-column options page (extends the column-display playbook step above). The Controller Support page added two things beyond "same rows, N styles, one-line tradeoff each" that made choosing much faster and belong in the step's template: (1) a **"try it in a table" preview** — a switcher that re-renders one sample table (real column names, ~7 rows including a null) in the selected style, because a style that looks fine in an isolated card can be too noisy down 25 rows; and (2) a **cost line per option** stating what it trades away (extra text shade, ambiguity of blank, needs color, etc.). Numbered cards so the developer can reply "5". Known weakness worth solving when this is built: the page re-implements each cell renderer in vanilla JS with copied tokens, so it can drift from the app (e.g. it used an amber the app has no token for). Rendering the options from the app's real components — e.g. as Storybook stories, since Storybook is already in the repo — would remove that drift and let the winner be copied straight out | Same session as the column-display playbook step. Build the template from the pages of both columns explored so far (Max Players, Controller Support) plus any that follow; don't design it from this note alone |
| Link cell as a documented pattern — an external URL shown as a small outline button (`Button variant="outline" size="sm"` with `nativeButton={false}` and `render={<a href={url} target="_blank" rel="noreferrer" />}`, label "Open" plus a trailing `ExternalLinkIcon`), chosen over eight other styles (generic text link, store name from the domain, raw domain, icon-only, store badge, hover-reveal, open+copy, name-as-link). Non-obvious bits a future agent would get wrong: (1) the `render={<a />}` + `nativeButton={false}` form is what keeps it a real anchor styled as a button (a `<button>` wrapping an `<a>` is invalid, and Base UI warns without `nativeButton={false}`); (2) every row shows the same visible word "Open", so give each an `aria-label` naming the row (`Open ${name} store page`) — the visible text stays inside the accessible name; (3) the null case must render the shared em-dash `emptyCell`, not a disabled button. Cost of the choice: it is the loudest link style and reads as a wall of buttons down a long table — the alternatives (store name/badge) need a domain-to-name map the app would own. Reference: the `link` column in `src/routes/games/games-columns.tsx` in the Game List app, `master`, commit `3694aba` (option 6 of 9 on the Link column exploration page) | Same session as the column-display playbook step and the cell-pattern entries above — write it as the "link" entry of that catalog (the playbook step already lists link as a candidate pattern). Don't extract a component: it's ~10 lines of markup over shadcn's `Button`. Also a data point for the options-page template item: this page put an empty-value row in every mini table, which surfaced the null case for free — keep that in the template |
| Enum → tone-mapped outline `Badge` as a documented cell pattern — a `Record<Enum, 'outline' \| 'outline-success' \| 'outline-destructive'>` lookup that groups a many-valued enum into good / neutral / bad and renders `<Badge variant={MAP[value]}>{SHORT_LABEL[value]}</Badge>`. Two columns in the Game List app now use it: Status (`STATUS_BADGE_VARIANT`) and Crossplay (`CROSSPLAY_BADGE_VARIANT`, five values → Full green, both "No" values red, Planned/Unconfirmed neutral). Crossplay was picked from an eight-style options page (plain, solid, outline, tinted chip, dot, icon + text, icon only, verdict + detail) — the developer chose outline because it matched the Status column, i.e. consistency with existing columns drove the choice as much as the style itself. The non-obvious part worth documenting is the *grouping* step, not the markup: decide which values collapse into which tone before choosing a style (Crossplay has two "No" and two "not yet known" values that look distinct as text but should share a tone), and keep a separate short-label map because the API enum strings are full sentences (`"Yes – Full Crossplay"`). Uses the local `outline-success`/`outline-destructive` `Badge` variants (see the status-color item above) — this is a second consumer for them, so it is also evidence for adding them to the foundation's `badge.tsx`. Reference: the `crossplay` column in `src/routes/games/games-columns.tsx` in the Game List app, `master`, commits `3374e64` (change) / `d686922` (merge) | Same session as the column-display playbook step and the cell-pattern entries above — write it as the "enum → colored outline badge" catalog entry, and pair it with the status-color tokens item so the `outline-*` variants land together with their `*-text` tokens and axe coverage. Don't extract a component: the lookup maps are per-entity, only the pattern is reusable. Also a data point for the options-page template item: opening the page with a short "value grouping" summary (how many values mean yes / no / unknown) made the tone decisions obvious before any style was shown — worth adding to the template |
| Summary-in-cell, detail-in-popover as a documented pattern — a one-to-many value (Game List's Ratings column: each game has 0..n raters, each with 1–5 stars) shown as one compact summary (`★ 4.7 (3)`) whose trigger opens the per-item breakdown. Chosen over nine other styles (initials + stars per person — the previous cell, initials + number chips, average + count, partial-fill average stars, tinted score pill, avatar stack, dot plot, distribution bars, segment meter). Non-obvious bits a future agent would get wrong: (1) use shadcn's `Popover` with `<PopoverTrigger openOnHover delay={100}>`, **not** `Tooltip` — a tooltip can't hold rich content and is unreachable on touch, and the trigger here should be a real `<button>` (it opens something), unlike the read-only icon-only cell above which deliberately uses `render={<span role="img" />}`; (2) shadcn's `hover-card` (Base UI `PreviewCard`) exists but is a hover/focus preview with nothing to tap on touch, so it was deliberately not used for information that isn't available elsewhere in the row — worth stating in the pattern so an agent doesn't "fix" it to `hover-card` under the never-hand-roll-what-shadcn-has rule (the Popover is already shadcn's, used as-is); (3) `PopoverContent` defaults to `w-72`, override with `w-auto min-w-48` for a short list; (4) the popup is portaled, so Playwright must query `page.getByRole('dialog')`, not `row.getByText(...)` — the Game List's `e2e/game-ratings.spec.ts` needed that change; (5) give the trigger an `aria-label` carrying the summary in words (`Average rating 4.5 from 2 ratings, show who rated Valheim`), and keep the empty case as the shared em-dash `emptyCell` with no trigger. Cost of the choice: the per-person detail is one hover away, so it can't be scanned down the column, and an average can't be sorted server-side without a backend change (the column stays `enableSorting: false`). Deliberately not a component — the summary math and popover rows are per-entity; only the wiring is reusable. Reference: `GameRatingsSummary` in `src/routes/games/game-rating-stars.tsx`, used by the `ratings` column in `src/routes/games/games-columns.tsx` in the Game List app, `master`, commit `84ff795` (option 10 of 10 on the Ratings exploration page) | Same session as the column-display playbook step and the cell-pattern entries above — write it as the "summary + popover detail" catalog entry, beside "icon only, label in tooltip" (the two are easy to confuse). Wait for a second one-to-many column (e.g. a reviewers or platforms list) before extracting anything |
| More data points for the options-page template item (Ratings column exploration, 10 options). (1) **Sample rows should span the value's cardinality, not just its values**: the page used five games with 3, 2, 1, 0 and 4 raters, so every option was judged on "split opinion", "single rater" and "nobody" at once, and the empty row again surfaced the null case for free. (2) **A row of per-option capability tags** (✓ shows who rated / ✓ shows disagreement / ✓ still fits 5+ raters) let the developer compare options on what each *preserves* rather than how it looks — for a multi-value column that is the real decision, and it complements the one-line cost line. (3) **A backend-implication note where it applies** ("sorting by average needs a backend change — ratings aren't sortable server-side today"): a backend developer reads this as a cost the way a designer reads visual weight, and it kept an unbuildable option from winning on looks alone. (4) Tone thresholds (the tinted-score option used 4.0 / 3.0 as placeholders) should be flagged on the card as a product decision, not baked in silently. (5) A hover/interaction option can be demoed live on the page (CSS `:hover`/`:focus`), which made option 10 easy to judge; a static screenshot would have hidden its whole point | Same session as the options-page template item above — fold these into that template rather than treating them as a separate task |
| Relative date as a documented cell pattern — a date column shown as time-from-today (`in 8 days`, `tomorrow`, `3 months ago`, `2 years ago`) with the exact date in the cell's `title`, chosen over seven other styles (absolute + "Releasing soon" badge, date + relative subline, countdown chip, ISO monospace, calendar tile, timeline dot, precision that fades with distance) for Game List's `releaseDate` column. The useful question for that column is "how soon?", which the old absolute date made the reader compute. Non-obvious bits a future agent would get wrong: (1) `Intl.RelativeTimeFormat(undefined, { numeric: 'auto' })` gives `tomorrow`/`yesterday`/`today` for free, but it takes one unit, so the cell needs its own unit ladder — days under 2 weeks, weeks under ~2 months, months under 2 years, then years — or "in 44 days" and "in 730 days" read badly; (2) the API's date is a calendar day with no time (`format: date`), so parse it as `T00:00:00Z`, format with `timeZone: 'UTC'`, and count days from *local today rebuilt as UTC midnight* (`Date.UTC(now.getFullYear(), now.getMonth(), now.getDate())`) — subtracting `Date.now()` directly shifts the answer by a day for anyone west of UTC; (3) relative text goes stale, so any test or MSW fixture that pins a date now drifts: nothing in the repo freezes the clock (`page.clock` in Playwright would), and the Game List app's e2e suite never asserts this cell, so the change shipped with `verify` green and no coverage; (4) `title` is a mouse-only affordance — no keyboard or touch access to the exact date — so if the exact date matters, the tooltip pattern from the icon-only cell item above is the accessible version; (5) muting rows by status (dates are settled history once the game shipped) is per-entity, only the formatter is reusable. Cost of the choice: `2 years ago` is vague for shipped rows, and a relative column doesn't look sorted even though server-side sorting still works on the underlying date. Reference: `formatRelativeDays` and the `releaseDate` column in `src/routes/games/games-columns.tsx` in the Game List app, `master`, commit `38a3fd9` (option 2 of 8 on the Release Date exploration page). Also a shared helper candidate: a `formatRelativeDays` in `src/lib/` if a second date column (last played, added on) repeats it | Same session as the column-display playbook step and the cell-pattern entries above — write it as the "date, relative" catalog entry. Don't extract the helper until a second date column needs it; do decide the clock-freezing convention when the first e2e test needs to assert a relative string. Also a data point for the options-page template item: a date page needs a **pinned "today"** shown in its header and sample rows chosen relative to it (shipped long ago, shipped recently, days out, weeks out, months out, no date), otherwise the page's own relative strings rot. The developer picked a plain text style (relative) over the more visual ones (calendar tile, timeline dot) with no hesitation — worth including a boring text option in every page rather than only visual ones |
| No visual indicator that the app is running against MSW mocks rather than a real backend (`main.tsx`'s `VITE_API` check) — the mock "Dev User" account and its small fixed fixture look identical to a real logged-in session at a glance, which cost a real user real debugging time chasing what looked like a broken login before realizing they were on the Vite dev server (MSW) instead of the real backend origin. Fixed locally in the Game List app (`game-list` commit `e696dcb`): a `role="status"` banner (`src/components/app/mock-mode-banner.tsx`) rendered above the router in `main.tsx` whenever `VITE_API !== 'real'`, reading a shared `IS_MOCK_MODE` flag (`src/lib/mock-mode.ts`); covered by `e2e/mock-mode-banner.spec.ts`. Found while building the Game List app with a separate agent | The pattern graduates into this starter's own scaffold (`App.tsx`/`main.tsx`), so every consuming app gets the banner for free instead of each one re-discovering and re-fixing this |
| Categorical (non-status) color tokens — a palette for values that differ from each other but aren't better or worse, e.g. Game List's Genre column (Survival / ARPG / Shooter / Build / Army / Action / RPG). The status-color item above covers tones that *mean* something (success / warning / destructive); this is a separate need, and `theme.css` has nothing for it (`--chart-1..5` are grays). Game List built its own: per genre a `--genre-<name>-700` (light) and `-400` (dark) primitive at one oklch hue, a semantic `--genre-<name>` remapped in `:root`/`.dark`, and a `--color-genre-<name>` line in `index.css`'s `@theme inline` — 35 lines across two files for one column, repeated for every new enum value or categorical column. Non-obvious bits a future agent would get wrong: (1) hues must avoid red and green, which success/destructive already spend, or a genre drawn in red reads as "bad"; (2) the tint goes on the glyph only and the label stays `text-foreground`, so contrast never depends on the hue — but axe checks text, not icon (non-text, 3:1) contrast, so the shades (oklch L 0.5 light / 0.78 dark, chroma ~0.12–0.15) were picked by eye and nothing automated covers them; (3) Tailwind needs whole class names, so the cell maps enum → `'text-genre-survival'` in a `Record` rather than building the class from the value; (4) with seven values, three hues land close together (violet / magenta / pink at 285 / 320 / 350), so the glyph shape, not the hue, is what tells those genres apart. Idea for the foundation: generic slots (`--category-1..8`, the same two-layer shape as `--chart-*`, both themes, one `@theme inline` line each) that an app maps enum → slot in its columns file, instead of a token per enum value. Unproven — one column has needed it. Reference: `src/styles/theme.css`, `src/index.css` and the `genre` column in `src/routes/games/games-columns.tsx` in the Game List app, `master`, commit `906c7a4` (option 9 of 19 on the Genre exploration page) | A second column wants categorical color, or the status-color tokens item above gets actioned — design categorical slots in that same pass so "meaningful color" and "just distinct color" are decided together. Don't add per-enum tokens to the foundation; add slots or nothing |
| Enum → icon + plain label as a documented cell pattern — a `Record<Enum, LucideIcon>` lookup rendered as `<span className="inline-flex items-center gap-1.5"><Icon className="size-4 shrink-0 …" aria-hidden />{label}</span>`. Chosen for Game List's Genre column from 11 styles (plain, outline badge, filled badge, icon + text, icon only, small caps, dot + text, tinted pill, colored icon + text, name subtitle, grouped rows), then a second page of 8 variations on the developer's shortlist (icon tile, colored tile, solid tile, icon in an outline badge, tinted pill with icon, name-on-hover, stacked, icon leading the name). The shortlist was all icon styles (neutral icon + text, icon only, colored icon + text) and the pick was colored icon + text. Non-obvious bits: (1) don't wrap it in `Badge` — a single-value enum in an outline badge looks identical to the multi-value Mechanics chips beside it and reads as one more tag; (2) the glyph is decorative next to a visible label, so `aria-hidden`, unlike the icon-only cell where `role="img"` plus `aria-label` is required; (3) lucide already has fitting glyphs for seven genres (`Flame`, `Sword`, `Crosshair`, `Hammer`, `Shield`, `Zap`, `BookOpen`), so check it before drawing anything — the Controller Support glyph had to be hand-drawn only because lucide has no half-filled gamepad; (4) every new enum value needs an icon or the row looks unfinished — typing the lookup as `Record<GameGenre, …>` makes `tsc` fail on a value added to the contract without one, which is the safer default over a silent fallback icon. Deliberately not a component; the map is per-entity. Reference: the `genre` column in `src/routes/games/games-columns.tsx` in the Game List app, `master`, commit `906c7a4` | Same session as the column-display playbook step and the cell-pattern entries above — write it as the "icon + label" entry beside "icon in a badge" and "icon only, label in tooltip", which together are the three icon styles. Also a signal for the catalog's ordering: the icon-in-badge (Max Players), icon-only (Controller Support) and icon + label (Genre) picks all went to icons over plainer styles, though the Release Date pick went plain — wait to see whether icons are a stable preference before ranking them first |
| More data points for the options-page template item (Genre column exploration, 19 options across two pages). (1) **Group options by what they cost the theme**, not by look: "fits as-is" / "needs new color tokens" / "removes the column", with a tag on each card. In a grayscale theme whose Hard Rule bans palette colors, whether color is worth new tokens *is* the decision, and grouping put it on the page before any style was judged. (2) **A view switcher — "all N values" vs "in the table"**: a strip of every enum value in each style shows the whole vocabulary at once, and the mini table (one row per value, so every value appears) shows what it does to density and to the neighbouring columns. The table view exposed that an outline badge collides with the Mechanics chips next to it, which the strip never would. (3) **Options that remove the column belong on the page** (genre as a subline under the name, rows grouped under a genre header) with their cost stated in DataTable terms: no column to sort by, and grouping fights column sorting and server-side pagination. (4) **Open with why this column differs from the last** ("one value, no good/bad axis, so color has nothing to say by default") — the same job as the value-grouping summary the Crossplay page opened with. (5) **A second round on the shortlist didn't change the outcome**: the developer shortlisted 3 of 11, asked for more, got 8 variations (options 12–19), and then picked option 9 from the first page. Offer a second round when asked, don't build it into the template by default. Appending the new options at the end rather than renumbering kept the numbers the developer had already used stable, and a "Your pick" tag on the shortlist kept them findable. (6) **More drift evidence for the Storybook idea in the format item above**: this page again re-implemented the cells with copied tokens — its text shade (oklch L 0.42) differs from what the app shipped (0.5) — and drew its lucide icons from SVG paths typed from memory, which can silently differ from the real glyphs. Rendering from the app's own components would remove both | Same session as the options-page template item — fold these into that template rather than treating them as a separate task. Item (2)'s switcher and item (1)'s cost grouping are the two worth building in first; (5) is a note to leave the second round out |
| Multi-value enum cell as a documented pattern — a column holding several values from one enum (Game List's Mechanics: 0..14 of 14 values, ordered, first = primary) shown **clustered by category, one icon per cluster**: `[Box] Build · Craft  [Swords] Horde`, each cluster a `<Badge variant="secondary" title={familyLabel}>` (existing shadcn variant — gray fill, no new component) holding the family's lucide icon (`aria-hidden`, tinted) and its values joined by a muted `·`. Chosen from a 9-option page (outline badges, primary badge + quiet rest, plain text, hashtags, primary + count, cap at two + overflow chip, family dots, family-sorted badges, click-to-filter chips) and then an 8-option second round on the developer's shortlist; final pick was two of the second round (family summary + names, and clusters), implemented as clusters. Non-obvious bits: (1) the taxonomy — a `Record<Enum, Family>` — is a **display decision made in the app's columns file, not in the API contract**; typing it as `Record<GameMechanic, …>` makes `tsc` fail when `openapi.yaml` gains a value without a family, the same safety as the enum → icon map. (2) Clustering **discards the picked order**, so the "first = primary" meaning disappears from the table — say so in a comment, and note the edit form still preserves it; the alternative that keeps order (icon per badge) repeats up to N icons in a row. (3) Multi-value cells need the empty case (`—`) and a wrap policy (`flex flex-wrap gap-1`) — a 6-value row (Warframe-like) is the stress case, so put one on the options page. (4) Overflow-collapse options (`+N` chip, primary + count) lost to showing everything; they hide the very thing a Mechanics column is for. (5) Existing tests that matched each value as its own element (`getByText('Roguelike', { exact: true })`) break when values share a badge — assert `toContainText` on the row instead. Reference: the `mechanics` column, `MECHANIC_FAMILY` / `MECHANIC_FAMILIES` in `src/routes/games/games-columns.tsx` in the Game List app, `master`, commit `d8121f1` | Same session as the column-display playbook step and the other cell-pattern entries — write it as the "multi-value enum" catalog entry, the one shape none of the single-value entries (icon in badge, icon only, icon + label, tone-mapped badge) covers. Don't extract a component: the family map and icons are per-entity, only the cluster markup is reusable |
| More data points for the options-page template item (Mechanics column exploration, 17 options across two pages) and for the categorical-color-tokens item. (1) **A rejected hue is evidence for that item's "avoid red and green" rule.** The page's Fight family used a red (oklch hue 25) as a faint badge background and the developer asked for it to be gray — it read as "dropped/bad" next to the Status column's red. The shipped version keeps the family hue on the glyph only and fills the badge with neutral `secondary`; the four `--family-*` hues (230 / 50 / 95 / 300, same 0.5-light / 0.78-dark lightness pair as `--genre-*`) are in `src/styles/theme.css` in the Game List app, `master`, commit `d8121f1`. That is a second categorical set after Genre — 11 tokens now, every one hand-tuned — which is the "a second column wants categorical color" trigger the categorical-tokens item names; if it is actioned, `--category-1..8` slots would have covered both columns. (2) **Global toggles on the options page beat per-card ones**: the second round had an *Order* switch (as picked / by family) and a *Grayscale* checkbox that applied to every card at once. Grayscale (`filter: grayscale(1)` on the preview) is the cheap colorblind check for any color-coded style and it settled which options depend on hue alone — worth building in for every color-coding column. Order matters for any ordered multi-value column. (3) **A taxonomy the agent invented must be visible and flagged as a product decision** — a legend on the page listing every value's family, with a note that the grouping is a guess. Same reasoning as the "tone thresholds are placeholders" note in the Ratings entry; the grouping is the thing the developer may want to change, not the styling. (4) **Drift again, and it changed the outcome**: the page invented a tinted-fill treatment (`color-mix` of the family hue into the background) that the app has no token or `Badge` variant for, and the developer's pick was "like 6 but gray" — i.e. the app's existing `secondary` fill. Options that use a fill the design system doesn't already have get judged as something the app can't render; another argument for rendering the options from the app's real components (Storybook) instead of copied tokens. (5) **Sample rows again spanning cardinality**: 1, 2, 3, 5, 6 values and an empty row — the 6-value row is what killed the one-badge-per-value styles | Same session as the options-page template item — fold these into that template. Item (2)'s two toggles and item (3)'s legend are the additions worth building in; (1) is a decision input for whoever designs the categorical slots, not a separate task |
| **Expand the design system with semantic color — decision, not just a gap.** The status-color item above has its trigger ("a consuming app decides it actually wants color-coded labels"); the Game List app has, on three columns (Status, Recent Steam Reviews, Crossplay). The developer explicitly does not want the design system to stay basic (grayscale + `destructive`) and picked three label styles from the Crossplay options page: **Solid badge** (`bg-{color} text-white`), **Outline badge** (`border-{color}-text text-{color}-text`) and **Tinted chip with icon** (`bg-{color}/13% text-{color}-text` + a lucide icon, squarer than a pill). All three share one token set, so the recommendation is to design them together: (1) `success`, `destructive` and `warning` tones, each with a darker `*-text` shade in light and dark and axe coverage in `e2e/a11y.spec.ts`; (2) `Badge` variants for outline and tinted per tone, plus a solid variant whose destructive/warning fills pass AA (the shipped `destructive` badge is ~3.8:1 and fails); (3) documentation of when each style fits. Honest caveats to weigh when actioning: Outline is proven (two columns) while Tinted and Solid are *wanted, not shipped* — Solid was not chosen for any column and reads as a red/green wall when a value has two "bad" members (Crossplay); registry-shipped changes need `scripts/consume-test.sh` against a commit SHA. Also a principle to carry into `docs/ARCHITECTURE.md`: "build against real need" should gate new *components and per-entity logic*, not the design language itself — tokens and tones are the part apps can't cheaply get right on their own, which is what a foundation is for. Reference: the Game List app, `master` — `src/styles/theme.css` (`--success-text`, `--destructive-text`, `--warning-text`), `src/components/ui/badge.tsx` (`outline-success`, `outline-destructive`), Crossplay column commits `3374e64`/`d686922`; the Crossplay options page (styles 2, 3, 4 of 8) | Pick this up together with the status-color and warning-tone items above — they are one piece of work. Start from the app's existing tokens and axe-checked shades rather than re-deriving them; decide Tinted/Solid at that point (add now, or record as optional) |

## Excluded

- **npm package** — forfeits open-code editability, the reason for this stack
- **Custom primitives** — shadcn's are already yours to edit
- **Theme switcher UI** — build the token architecture, not the feature
- **SSR / SEO tooling** — irrelevant for personal database applications
